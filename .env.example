# MongoDB Configuration
MONGO_URL=mongodb://localhost:27017/prism_db

# JWT Configuration
JWT_ACCESS_SECRET=your-access-secret-key-here-change-this
JWT_REFRESH_SECRET=your-refresh-secret-key-here-change-this
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Email Configuration
FROM_EMAIL=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here
TO_EMAIL=recipient-email@gmail.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# CORS Configuration (comma-separated list of allowed origins)
# Add all frontend URLs that will access the API
# Examples: http://localhost:3000,http://localhost:5173,http://192.168.1.100:5173
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# PayPal (for buy-credits)
# PAYPAL_CLIENT_ID=your-paypal-client-id
# PAYPAL_CLIENT_SECRET=your-paypal-client-secret
# PAYPAL_MODE=sandbox

# Razorpay (for buy-credits in INR)
# RAZORPAY_KEY_ID=your-razorpay-key-id
# RAZORPAY_KEY_SECRET=your-razorpay-key-secret
# USD_TO_INR_RATE=83

# AWS S3 (prism-bucket-10 in ap-south-2) - Object URLs only, no presigned
# S3_BUCKET_NAME=prism-bucket-10
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_REGION=ap-south-2

# FFmpeg (optional - converts annotated camera video to H.264 for browser playback)
# Download: https://www.gyan.dev/ffmpeg/builds/ (ffmpeg-release-essentials.zip)
# Extract ffmpeg.exe to backend/ffmpeg-8.0.1/bin/ or set full path:
# FFMPEG_PATH=C:\path\to\ffmpeg.exe

# LinkedIn (org-only: owner connects, job post to LinkedIn)
# LINKEDIN_CLIENT_ID=your-linkedin-client-id
# LINKEDIN_CLIENT_SECRET=your-linkedin-client-secret
# Local (HTTP): http://localhost:5555/api/oauth/linkedin/callback
# Production (HTTPS): https://your-backend-host/api/oauth/linkedin/callback
# LINKEDIN_REDIRECT_URI=http://localhost:5555/api/oauth/linkedin/callback
